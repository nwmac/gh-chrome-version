name: Tests
on:
  push:
    branches:
      - master
      - 'release-*'
  pull_request:
      branches:
        - master
        - 'release-*'
jobs:
  e2e-test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Installed chrome version"
          google-chrome --version

